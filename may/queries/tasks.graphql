query getTasks {
  allTasks {
    edges {
      node {
        id
        name
      }
    }
  }
}

mutation addTask($task: TaskCreateMutationInput!) {
  addTask(input: $task) {
    id
    name
  }
}
